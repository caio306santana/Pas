{
  "name": "pastel-delivery-saas",
  "version": "1.0.0",
  "description": "Multi-tenant SaaS delivery platform for Pastel, Churros and Drinks",
  "scripts": {
    "infra:up": "docker-compose up -d",
    "infra:down": "docker-compose down",
    "backend:dev": "npm run start:dev --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
